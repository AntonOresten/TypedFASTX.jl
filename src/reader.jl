abstract type AbstractReader{T} end

# QualityScores

# Define Base.getindex(reader::TypedFASTQRecord{T}...) using ReadDatastores?

# read_fasta and read_fastq calls AbstractReader accordingly
# readdatastore field?
# readdatastores creates fastq ds file. do we need to change the functions?